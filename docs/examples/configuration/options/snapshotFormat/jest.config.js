const config = {
  snapshotFormat: {
    printBasicPrototype: false,     // | Object & Array, ❌NOT print a prefix❌
  },
};

console.log("configuration - options - snapshotFormat ", config);
module.exports = config;
